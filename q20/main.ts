
let User_info:{name: string,age: number,isStudent: boolean} = {
    name:"Kamran",
    age:19,
    isStudent:true
}
console.log(`my name is ${User_info.name} and my age is ${User_info.age} and I am a  Student.${User_info.isStudent}`);
